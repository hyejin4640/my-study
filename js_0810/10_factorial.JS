/*
5! = 5*4*3*2*1 =120
입력:5
 */

let mulSum = 1;
for(let i =1; i<=5; i++){
  mulSum *=i;
}
console.log(mulSum);

/**
 * 10의 약수 출력
 * 약수 : 임의의 수를 나누었을때 나누어 떨어지는 수 : 나머지가0인수
 * 소수 : 1과 자기자신으로 나누어 떨어지는수 : 1과자기자신외에는 약수를 가지지 않는수
 */

let ten = 10;
for(let i =1; i<=ten; i++){
  if(ten % i ===0){
    console.log(`약수 : ${i}`);
      }
    }



/**
 * 소수구하기 : 2,3,5,7
 * 1-10까지 숫자중소수
 *  소수 : 1과 자기자신으로 나누어 떨어지는수 : 1과자기자신외에는 약수를 가지지 않는수
 */

// ---2부터 시작할경우
// for(let i = 2; i<=10; i++){
//   let count =0;
//   for(let j =2; j<=10; j++){
//     // 1-10숫자중에서 2부터 나누면 1과 나누어 떨어지는거는 조건에 아예 빼고 자기자신과만 나누어떨어지는지만 확인하면 된다.
//     // 그래서 밑에 count에도 1과같으면 소수라는 걸로 조건을 걸어주었다
//     if(i % j === 0){
//       count ++;
//     }
//   }
//   // console.log(`카운트${count}`);
//   if(count === 1){
//     //1 과 자기자신으로 나누어떨어지는수가 소수이니까 1-10중에 2번 나누어떨어지면 소수???
//     console.log(`소수 : ${i}`);
//   }
// }

// ---1부터 시작할경우
for(let i = 0; i<=10; i++){
  let count =0;
  for(let j =0; j<=10; j++){
    if(i % j === 0){
      count ++;
    }
  }
  // console.log(`카운트${count}`);
  if(count === 2){
    //1 과 자기자신으로 나누어떨어지는수가 소수이니까 1-10중에 2번 나누어떨어지면 소수???
    console.log(`소수 : ${i}`);
  }
}

// 일단 2로예를들면 2를1로나누면 0이고 2를2로 나누면 0이니까 나머지 값들을 나누어서 0이아니라면 총2번 밖에 0으로 나누어떨어지는 수는 없으니까 소수가되고
// 자기자신과 1로 떨어지는것도 알수있다